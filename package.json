{
  "name": "nlw-5-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "json-server server.json -d 750 -p $PORT --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
